const VISA = {
  LONG_STAY: {
    RELIGIOUS: {
      RELIGIOUS_D: {
        typology: 0,
        category: 0,
        visa: 0,
      },
    },
    STUDY: {
      TIROCINIO: {
        typology: 0,
        category: 1,
        visa: 0,
      },
      STUDY_D: {
        typology: 0,
        category: 1,
        visa: 1,
      },
    },
    FAMILY: {
      SPOUSE: {
        typology: 0,
        category: 2,
        visa: 0,
      },
      PARENTS: {
        typology: 0,
        category: 2,
        visa: 1,
      },
      CHILDREN: {
        typology: 0,
        category: 2,
        visa: 2,
      },
    },
    EMPLOYMENT: {
      EMPLOYMENT_D: {
        typology: 0,
        category: 3,
        visa: 0,
      },
    },
    RE_ENTRY: {
      RE_ENTRY: {
        typology: 0,
        category: 4,
        visa: 0,
      },
    },
    MISSION: {
      MISSION: {
        typology: 0,
        category: 5,
        visa: 0,
      },
    },
  },
  AIRPORT_TRANSIT: {},
};

const LOCATION = {
  CAIRO: 0,
  ALEX: 1,
};

const EMPLOYMENT_D_CAIRO = [
  LOCATION.CAIRO,
  VISA.LONG_STAY.EMPLOYMENT.EMPLOYMENT_D,
];
const FAMILY_CHILDREN_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.FAMILY.CHILDREN];
const FAMILY_PARENTS_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.FAMILY.PARENTS];
const FAMILY_SPOUSE_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.FAMILY.SPOUSE];
const MISSION_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.MISSION.MISSION];
const RELIGIOUS_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.RELIGIOUS.RELIGIOUS_D];
const RE_ENTRY_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.RE_ENTRY.RE_ENTRY];
const STUDY_D_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.STUDY.STUDY_D];
const TIROCINIO_CAIRO = [LOCATION.CAIRO, VISA.LONG_STAY.STUDY.TIROCINIO];

const EMPLOYMENT_D_ALEX = [
  LOCATION.ALEX,
  VISA.LONG_STAY.EMPLOYMENT.EMPLOYMENT_D,
];
const FAMILY_CHILDREN_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.FAMILY.CHILDREN];
const FAMILY_PARENTS_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.FAMILY.PARENTS];
const FAMILY_SPOUSE_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.FAMILY.SPOUSE];
const MISSION_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.MISSION.MISSION];
const MISSION_GRATIS_ALEX = [
  LOCATION.ALEX,
  VISA.LONG_STAY.MISSION.MISSION_GRATIS,
];
const RELIGIOUS_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.RELIGIOUS.RELIGIOUS_D];
const RE_ENTRY_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.RE_ENTRY.RE_ENTRY];
const STUDY_D_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.STUDY.STUDY_D];
const TIROCINIO_ALEX = [LOCATION.ALEX, VISA.LONG_STAY.STUDY.TIROCINIO];

const visaEnums = {
  EMPLOYMENT_D_CAIRO: EMPLOYMENT_D_CAIRO,
  FAMILY_CHILDREN_CAIRO: FAMILY_CHILDREN_CAIRO,
  FAMILY_PARENTS_CAIRO: FAMILY_PARENTS_CAIRO,
  FAMILY_SPOUSE_CAIRO: FAMILY_SPOUSE_CAIRO,
  MISSION_CAIRO: MISSION_CAIRO,
  RELIGIOUS_CAIRO: RELIGIOUS_CAIRO,
  RE_ENTRY_CAIRO: RE_ENTRY_CAIRO,
  STUDY_D_CAIRO: STUDY_D_CAIRO,
  TIROCINIO_CAIRO: TIROCINIO_CAIRO,
  EMPLOYMENT_D_ALEX: EMPLOYMENT_D_ALEX,
  FAMILY_CHILDREN_ALEX: FAMILY_CHILDREN_ALEX,
  FAMILY_PARENTS_ALEX: FAMILY_PARENTS_ALEX,
  FAMILY_SPOUSE_ALEX: FAMILY_SPOUSE_ALEX,
  MISSION_ALEX: MISSION_ALEX,
  MISSION_GRATIS_ALEX: MISSION_GRATIS_ALEX,
  RELIGIOUS_ALEX: RELIGIOUS_ALEX,
  RE_ENTRY_ALEX: RE_ENTRY_ALEX,
  STUDY_D_ALEX: STUDY_D_ALEX,
  TIROCINIO_ALEX: TIROCINIO_ALEX,
};
